import 'package:flutter/material.dart';

import 'package:weekday_selector/weekday_selector.dart';

class daysOfWeekWidgetDisplay extends StatelessWidget {
  @override
  final String days;

  daysOfWeekWidgetDisplay(this.days);

  bool _daysStringToBool(days) {
    return days;
  }

  Widget build(BuildContext context) {
    return Container();
  }
}
